$('#unsubscribe-link').remove()
$('.subscription-links').html(
  '<%= link_to 'Subscribe', question_subscriptions_path(@subscription.question), 
        method: :post, 
        remote: true, 
        id: 'subscribe-link' %>')
